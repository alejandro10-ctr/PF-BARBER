const {Router} = require('express')
const {Appointment} = require('../db.js')

const router = Router();

router.get('/' (req,res) => {
    try {
        
    } catch (error) {
        console.log(error)
    }
})