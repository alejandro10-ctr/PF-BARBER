/* const ProductItem = ({data, addToCart})=> {

    let {id, name, price, image} = data;  // => poner los atributos necesarios de los nuevos productos

    return (
        <div> */
           {/*  <img> {image} </img> */}
           /*  <h4>{name}</h4>
            <h5>${price}</h5>
            <button onClick={()=> addToCart(id)} >Add</button>
        </div>)
    
};

export default ProductItem; */